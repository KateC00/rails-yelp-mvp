<h1>Restaurants:</h1>

<ol>
  <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name %> - <%= restaurant.address %> <%= link_to "+", restaurant_path(restaurant) %></li>
  <% end %>
</ol>
